import React from 'react';
import { Text, View } from 'react-native';

const ComponentOne = props => (
    <View style={{ flexDirection: 'row', marginTop: 90 }}>
        <Text>Example ComponentOne!</Text>
    </View>
);

export default ComponentOne;