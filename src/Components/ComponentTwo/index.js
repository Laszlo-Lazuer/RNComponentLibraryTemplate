import React from 'react';
import { Text, View } from 'react-native';

const ComponentTwo = props => (
    <View style={{ flexDirection: 'row', marginTop: 90 }}>
        <Text>Example ComponentTwo!</Text>
    </View>
);

export default ComponentTwo;